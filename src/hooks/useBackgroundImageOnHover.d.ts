/**
 * A React hook that adds background image hover effects to elements with data-image-hd attribute
 * When hovering over an element with data-image-hd, displays that image as a subtle background overlay
 * @returns void
 */
export function useBackgroundImageOnHover(): void;
